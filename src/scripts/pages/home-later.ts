// The home page's second wave: the journey deck and labels (needed once the scene is up) and
// the sections further down. Imported in parallel with the scene, so none of it sits in the
// entry chunk ahead of first paint.
export { JourneyUI } from "../atlas/ui/journey";
export { Labels } from "../atlas/labels";
export { initLogbook } from "../atlas/ui/logbook";
export { initTickets } from "../atlas/ui/tickets";
export { footerArc } from "../atlas/ui/footer";
